# Oku API base URL (no trailing slash). Required for waitlist form — same API you deploy (ECS).
# Build-time: set in CI or `export` before `npm run build`.
NEXT_PUBLIC_OKU_API_URL=https://api.example.com
