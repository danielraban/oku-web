# Neon Postgres — auto-provisioned when you add the Neon integration in Vercel
# (`vercel integration add neon` or Storage tab in the dashboard).
# Locally: `vercel env pull .env.local`
DATABASE_URL=
